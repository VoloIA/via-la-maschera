# Check 8: Base Iniziale App

Obiettivo di questo checkpoint:

Sostituire la schermata demo di Expo con una prima base nostra.

## Cosa abbiamo cambiato

La Home ora mostra:

- nome `AppMobile`;
- titolo `La mia prima app`;
- stato progetto;
- percentuale verso la pubblicazione store;
- passaggi completati;
- prossima decisione.

La seconda tab ora si chiama:

```text
Percorso
```

e mostra:

- checkpoint completati;
- prossime fasi;
- percentuale attuale.

## File modificati

```text
C:\Users\intel i7 11700\Desktop\AppMobile\app\app\(tabs)\index.tsx
C:\Users\intel i7 11700\Desktop\AppMobile\app\app\(tabs)\explore.tsx
C:\Users\intel i7 11700\Desktop\AppMobile\app\app\(tabs)\_layout.tsx
C:\Users\intel i7 11700\Desktop\AppMobile\app\app.json
```

## Nome progetto

Nel file `app.json` abbiamo cambiato:

```text
name: AppMobile
slug: appmobile
scheme: appmobile
```

## Verifica tecnica

Comando eseguito:

```powershell
npm.cmd run lint
```

Risultato:

Nessun errore segnalato.

## Percentuale progetto

Stato attuale:

```text
15%
```

Il 100% rappresenta app pronta per pubblicazione su Play Store o App Store.

Siamo al 15% perche:

- ambiente pronto;
- progetto Expo creato;
- app avviata;
- Git configurato;
- prima base grafica creata.

Mancano ancora:

- idea definitiva della app;
- funzioni principali;
- dati o contenuti veri;
- test;
- icona e branding definitivi;
- account store;
- build Android/iOS;
- schede store e privacy.

